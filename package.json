{
  "name": "aegis-maritime-system",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,magenta \"npm --prefix apps/backend-api run dev\" \"npm --prefix apps/dashboard-next run dev\"",
    "build": "npm --prefix apps/dashboard-next run build",
    "start": "npm run build && npm --prefix apps/backend-api start",
    "stop": "kill-port 4000 5173",
    "stop:all": "npm run stop"
  },
  "dependencies": {
    "axios": "^1.15.0",
    "serialport": "^13.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "kill-port": "^2.0.1"
  }
}
